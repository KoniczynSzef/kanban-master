import React, { FC } from "react";

interface Props {}

const page: FC<Props> = () => {
    return (
        <div>
            <p>This is the dashboard page</p>
        </div>
    );
};

export default page;
